import { createSelector } from "@reduxjs/toolkit";
import { searchTextSelector } from "../components/SearchBox/searchSlice";

// const getCurrentPosts = createSelector(searchTextSelector, searchTextSelector, async () => {

// })